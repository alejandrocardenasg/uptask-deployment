doctype html
html(lang="en")
    head
        include ./partials/header
    body.login 

        .contenedor-formulario 
            h1= nombrePagina 

            p= correo + " - Cambia tu contraseña"
            form.caja-login(method="POST")
                .campo 
                    label(for="password") Nueva contraseña:
                    input(type="password" name="password" placeholder="Tu nueva contraseña" required) 
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer contraseña")